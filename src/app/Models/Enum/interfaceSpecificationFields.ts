export enum FieldTypes {
    ENUM,
    STRING,
    LIST,
    OPTION,
    OPTIONS
}

export enum Category {
    CATEGORY_01,
    CATEGORY_02,
    CATEGORY_03,
    CATEGORY_04
}

export enum Status {
    ENABLE,
    DISABLE,
    SUSPEND,
    DELETE,
}
